#include <iostream>

using namespace std;

int main(){

    // PRIMITIVE DATA TYPES

    //to store integer. no decimal or character value.
    int age = 20;   

    //to store alphabet can be capital or small letters. only store single alphabet in single quotes
    char grade = 'B';   

    //to store decimal value. 
    /*the f letter is used to signal compiler that i want to store float not double. 
    if not written the it assumes its double value. */
    /*here variable name are also in capital PI, its like unofficial rule that 
    constant value's variableName should be in capital letter */
    float PI = 3.14f;   
    float price = 79.83f;

    //to store value in true(1) or false(0) format
    bool ageLimit = true;
    bool ageLimit = false;

    //to store values in decimal points
    double balance = 1000.8765;

    return 0;
}