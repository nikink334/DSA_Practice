#include <iostream>

using namespace std;

int main(){

    // Variables = containers that store data in memory
    // Rules: its hould always start with underscore(_) or capital or small a to z(A-Z, a-z)
    // invalid name = 9age, @name, 89

    int age = 20;
    char alphabet = 'N';
    // in above example age and name are vairables.
    // to print just use the variable name

    cout << age << endl;
    cout << alphabet << endl;
    cout << "My aplhabet is " << alphabet << " and my age is " <<age << endl;

    return 0;
}