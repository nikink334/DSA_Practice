#include <iostream>

using namespace std;

int main(){

    // ternary means three
    // syntax: condition ? "true statement" : "false statement"
   
    int n = -45;
    cout << ( n>=0 ? "Positive" : "Negative") <<endl;


    return 0;
}